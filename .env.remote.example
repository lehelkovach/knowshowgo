# Remote Development Configuration
# Copy to .env.remote and fill in your values
#
# Usage:
#   cp .env.remote.example .env.remote
#   # Edit .env.remote with your values
#   ./scripts/remote-dev.sh status

# Required: OCI VM connection
OCI_SSH_HOST=123.45.67.89
OCI_SSH_USER=ubuntu
OCI_SSH_KEY=~/.ssh/id_ed25519

# Optional: SSH settings
OCI_SSH_PORT=22
OCI_APP_DIR=/opt/knowshowgo/repo

# Optional: API URL for health checks
REMOTE_API_URL=http://localhost:3000

# Optional: ArangoDB (if connecting directly)
ARANGO_URL=http://123.45.67.89:8529
ARANGO_DB=knowshowgo
ARANGO_USER=root
ARANGO_PASS=changeme
